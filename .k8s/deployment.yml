apiVersion: apps/v1
kind: Deployment
metadata:
  name: merlin
  labels:
    app: merlin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: merlin
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: merlin
    spec:
      containers:
        - name: merlin
          image: <IMAGE>
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 100m
            limits:
              cpu: 100m
---
apiVersion: v1
kind: Service
metadata:
  name: merlin-service
spec:
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: merlin